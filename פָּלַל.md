the sub department field is only applicable for the Letters options in the module DDL, if all is selected then the sub department options should not be available. when ALL is selected, it simply means all the sub departments are included. by default the menu design is going to be without any fields from sub departments. only after 'GO' button click it will appear based on whether the SUB DEPT is applicable for that scenario. right now sub department is editable before the GO button click. this is not the expected behavior. also on clicking the search button associated with the sub dept, it is leading to an error.